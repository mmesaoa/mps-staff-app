name: school_erp_staff_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # ✅ FIX: Aligned all Riverpod packages to compatible versions
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  dio: ^5.9.0
  flutter_secure_storage: ^9.2.4
  go_router: ^16.2.1
  intl: ^0.19.0 # Updated to a more recent compatible version
  file_picker: ^10.3.3
  url_launcher: ^6.2.6
  flutter_widget_from_html: ^0.15.1
  table_calendar: ^3.0.9
  local_auth: ^2.3.0
  
  # Location & Background Service
  permission_handler: ^11.3.0
  geolocator: ^11.0.0
  geolocator_android: ^4.6.0
  
  fl_chart: ^0.70.2
  image_picker: ^1.2.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0 # Updated to a more recent compatible version

  build_runner: ^2.4.11
  
  # ✅ FIX: Aligned the generator to a compatible version
  riverpod_generator: ^2.4.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/  